{
  "name": "pacto-teste-automacao",
  "version": "1.0.0",
  "description": "Testes automatizados em Cypress e Playwright para validação da busca no Yahoo.",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "playwright:test": "playwright test",
    "playwright:ui": "playwright test --ui"
  },
  "devDependencies": {
    "cypress": "^13.13.1",
    "@playwright/test": "^1.48.2"
  }
}